#include "cppsuckdef.h"

float g_scale = 1.f;
float g_offset_x = 0.f;
float g_offset_y = 0.f;
float g_width = static_cast<float>(SCREEN_WIDTH);
float g_height = static_cast<float>(SCREEN_HEIGHT);
float g_time_step = SEC_PER_UPDATE;
